<%= button_to "Create a New Playlist", new_playlist_path %>
<h1>All of Your Smooth Playlists</h1>

<ul>
  <% @playlists.each do |playlist| %>
    <li>
      <%= link_to playlist.name,  playlist_path(playlist) %>
    </li>
  <% end %>
</ul>
